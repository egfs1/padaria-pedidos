<%- include('../partials/header.ejs') %>

<div class="container">
    <hr>
    <div class="card">
        <div class="card-header">
            <h2>Editar Empresa</h2>
        </div>
        <div class="card-body">
            <form class="needs-validation" method="POST" action='/companies/update' novalidate>
                <input class='form-control' type="text" name='name' value='<%= company.name %>' placeholder='<%= company.name %>' required>
                <div class="invalid-feedback">
                    Nome inválido
                </div>
                <input type="hidden" name='id' value='<%= company.id %>'>
                <button class="btn btn-primary mt-4">Editar</button>
            </form>

        </div>
    </div>
</div>
<%- include ('../partials/footer.ejs') %>
<%- include ('../partials/form-validate.ejs') %>